import { create } from 'zustand'

// TODO 1: 今日の達成済み habitId の配列（doneIds）をstateとして定義する
// TODO 2: doneIds に habitId を追加するアクションを定義する
// TODO 3: doneIds から habitId を取り除くアクションを定義する

export const useHabitStore = create(() => ({}))
